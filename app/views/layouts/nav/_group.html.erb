<%= render layout: "layouts/nav/dropdown", locals: { 
      id: "group-#{group.title.parameterize}",
      icon: group.icon_uri || "folder",
      title: group.title,
      text: group.title
    } do %>
  <%= render partial: 'layouts/nav/group_items', locals: local_assigns %>
<% end %>